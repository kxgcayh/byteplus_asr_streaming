name: byteplus_asr_streaming
description: BytePlus ASR streaming WebSocket client
version: 0.1.0
publish_to: 'none'

environment:
  sdk: ^3.7.2
  flutter: '>=3.24.0'

dependencies:
  flutter:
    sdk: flutter
  record: ^6.2.1
  web_socket_channel: ^3.0.3
  uuid: ^4.5.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0